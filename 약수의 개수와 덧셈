function solution(left, right) {
    var answer = 0;
    for(let i=left; i<=right; i++){
        var num= Number.isInteger(Math.sqrt(i));
        answer+= (num ? -1 : 1)*i;
    }
    return answer;
}
