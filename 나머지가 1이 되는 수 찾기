function solution(n) {
    var answer = 0;
    var arr= [];
    for(let i=2; i<n; i++){
        if(n%i===1){
            arr.push(i);
            answer=Math.min.apply(null,arr);
        }
    }
    return answer;
}
