function solution(nums) {
    var answer = 0;
    // let set= new Set(nums);
    let set = [...new Set(nums)].length;
    let num= nums.length/2;
    

    console.log(set)
    
    return set> num ? num : set;
}
